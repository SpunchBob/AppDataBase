<Page x:Class="AppDataBaseView.pages.FlightsPages.FlightsPageChange"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AppDataBaseView.pages.FlightsPages"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="600"
      Title="FlightsPageChange">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/pages/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" 
                Background="{StaticResource PrimaryBlueBrush}" 
                BorderBrush="{StaticResource BorderBlueBrush}" 
                BorderThickness="1"
                CornerRadius="2"
                Margin="0,0,0,15">
            <TextBlock Text="ИЗМЕНЕНИЕ ПЕРЕВОЗКИ" 
                       Foreground="White" 
                       FontWeight="SemiBold"
                       FontSize="14"
                       HorizontalAlignment="Center"
                       Padding="10,8"/>
        </Border>

        <!-- Выбор перевозки -->
        <GroupBox Grid.Row="1"
                  Header="ВЫБОР ПЕРЕВОЗКИ" 
                  Style="{StaticResource DBeaverGroupBox}"
                  Margin="0,0,0,15">
            <Grid Margin="0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <ComboBox Style="{StaticResource DBeaverComboBox}"
                          x:Name="employees_cb"
                          Height="28"
                          VerticalAlignment="Center"
                          SelectionChanged="FlightsComboBox_SelctionChanged"
                          Cursor="Hand"/>
                <TextBlock Grid.Column="2"
                           Text="▼"
                           Foreground="{StaticResource PrimaryBlueBrush}"
                           FontWeight="Bold"
                           VerticalAlignment="Center"
                           Margin="0,0,5,0"/>
            </Grid>
        </GroupBox>

        <!-- Основная форма -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="0,10">
                <!-- Информация о заказчике -->
                <GroupBox Header="ИНФОРМАЦИЯ О ЗАКАЗЧИКЕ" 
                          Style="{StaticResource DBeaverGroupBox}"
                          Margin="0,0,0,15">
                    <StackPanel Margin="0,8">
                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Label Style="{StaticResource DBeaverLabel}"
                                   Content="Код заказчика:"
                                   VerticalAlignment="Center"/>
                            <TextBox Style="{StaticResource DBeaverTextBox}"
                                     x:Name="code_tb"
                                     Grid.Column="1"
                                     Height="28"
                                     VerticalAlignment="Center"
                                     IsEnabled="False"
                                     Background="#F5F5F5"/>
                        </Grid>

                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Label Style="{StaticResource DBeaverLabel}"
                                   Content="Заказчик:"
                                   VerticalAlignment="Center"/>
                            <TextBox Style="{StaticResource DBeaverTextBox}"
                                     x:Name="customer_tb"
                                     Grid.Column="1"
                                     Height="28"
                                     VerticalAlignment="Center"/>
                        </Grid>
                    </StackPanel>
                </GroupBox>

                <!-- Маршрут -->
                <GroupBox Header="МАРШРУТ" 
                          Style="{StaticResource DBeaverGroupBox}"
                          Margin="0,0,0,15">
                    <StackPanel Margin="0,8">
                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Label Style="{StaticResource DBeaverLabel}"
                                   Content="Откуда:"
                                   VerticalAlignment="Center"/>
                            <TextBox Style="{StaticResource DBeaverTextBox}"
                                     x:Name="from_tb"
                                     Grid.Column="1"
                                     Height="28"
                                     VerticalAlignment="Center"/>
                        </Grid>

                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Label Style="{StaticResource DBeaverLabel}"
                                   Content="Куда:"
                                   VerticalAlignment="Center"/>
                            <TextBox Style="{StaticResource DBeaverTextBox}"
                                     x:Name="Where_tb"
                                     Grid.Column="1"
                                     Height="28"
                                     VerticalAlignment="Center"/>
                        </Grid>
                    </StackPanel>
                </GroupBox>

                <!-- Даты -->
                <GroupBox Header="ДАТЫ" 
                          Style="{StaticResource DBeaverGroupBox}"
                          Margin="0,0,0,15">
                    <StackPanel Margin="0,8">
                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Label Style="{StaticResource DBeaverLabel}"
                                   Content="Дата отправления:"
                                   VerticalAlignment="Center"/>
                            <DatePicker Style="{StaticResource DBeaverDatePickerMinimal}"
                                        x:Name="send_date_tb"
                                        Grid.Column="1"
                                        Height="28"
                                        VerticalAlignment="Center"/>
                        </Grid>

                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Label Style="{StaticResource DBeaverLabel}"
                                   Content="Дата прибытия:"
                                   VerticalAlignment="Center"/>
                            <DatePicker Style="{StaticResource DBeaverDatePickerMinimal}"
                                        x:Name="arve_date_tb"
                                        Grid.Column="1"
                                        Height="28"
                                        VerticalAlignment="Center"/>
                        </Grid>
                    </StackPanel>
                </GroupBox>

                <!-- Детали перевозки -->
                <GroupBox Header="ДЕТАЛИ ПЕРЕВОЗКИ" 
                          Style="{StaticResource DBeaverGroupBox}"
                          Margin="0,0,0,15">
                    <StackPanel Margin="0,8">
                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Label Style="{StaticResource DBeaverLabel}"
                                   Content="Код груза:"
                                   VerticalAlignment="Center"/>
                            <ComboBox Style="{StaticResource DBeaverComboBox}"
                                      x:Name="load_code_cb"
                                      Grid.Column="1"
                                      Height="28"
                                      VerticalAlignment="Center"/>
                        </Grid>

                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Label Style="{StaticResource DBeaverLabel}"
                                   Content="Цена:"
                                   VerticalAlignment="Center"/>
                            <TextBox Style="{StaticResource DBeaverTextBox}"
                                     x:Name="price_tb"
                                     Grid.Column="1"
                                     Height="28"
                                     VerticalAlignment="Center"/>
                        </Grid>

                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Label Style="{StaticResource DBeaverLabel}"
                                   Content="Код сотрудника:"
                                   VerticalAlignment="Center"/>
                            <ComboBox Style="{StaticResource DBeaverComboBox}"
                                      x:Name="employee_code_cb"
                                      Grid.Column="1"
                                      Height="28"
                                      VerticalAlignment="Center"/>
                        </Grid>
                    </StackPanel>
                </GroupBox>

                <!-- Статусы -->
                <GroupBox Header="СТАТУСЫ" 
                          Style="{StaticResource DBeaverGroupBox}"
                          Margin="0,0,0,15">
                    <StackPanel Margin="0,8">
                        <CheckBox Style="{StaticResource DBeaverCheckBox}"
                                  x:Name="is_bought_rb"
                                  Content="Отметка об оплате"
                                  Margin="10,4"/>
                        <CheckBox Style="{StaticResource DBeaverCheckBox}"
                                  x:Name="is_refund_rb"
                                  Content="Отметка об возврате"
                                  Margin="10,4"/>
                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>

        <!-- Панель действий -->
        <Border Grid.Row="3" 
                Background="{StaticResource LightBlueBrush}"
                BorderBrush="{StaticResource BorderBlueBrush}"
                BorderThickness="0,1,0,0"
                Margin="0,10,0,0"
                Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Style="{StaticResource DBeaverButton}"
                        x:Name="change_btn"
                        Grid.Column="1"
                        Content="Изменить"
                        Width="120"
                        Height="35"
                        Cursor="Hand" 
                        Click="change_btn_Click"/>
            </Grid>
        </Border>

        <!-- Информационная метка -->
        <Label x:Name="info_lb" 
               Grid.Row="2"
               Grid.RowSpan="2"
               FontSize="14"
               VerticalAlignment="Bottom"
               HorizontalAlignment="Center"
               Margin="0,0,0,60"
               Background="#E8F4FD"
               Padding="10,5"
               BorderBrush="{StaticResource BorderBlueBrush}"
               BorderThickness="1"
               Visibility="Collapsed"/>
    </Grid>
</Page>